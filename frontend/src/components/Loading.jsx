function Loading() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "300px",
        color: "white",
        fontSize: "22px",
      }}
    >
      ⏳ Loading...
    </div>
  );
}

export default Loading;
